"use client";import {useMemo,useState} from "react";import {PRODUCTS} from "../lib/products";import {ProductCard} from "./ProductCard";
export function ProductGrid(){const[cat,setCat]=useState("All");const[q,setQ]=useState("");const[sort,setSort]=useState("featured");const list=useMemo(()=>{let a=PRODUCTS.filter(p=>(cat==="All"||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase()));return [...a].sort((x,y)=>sort==="low"?x.price-y.price:sort==="high"?y.price-x.price:0)},[cat,q,sort]);return <><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products..."/><select value={cat} onChange={e=>setCat(e.target.value)}><option>All</option><option>LED Strip Lights</option><option>RGB-IC Neon Lights</option><option>Accessories</option></select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="low">Price: Low to High</option><option value="high">Price: High to Low</option></select></div><div className="shopbar"><b>{list.length} products</b></div><div className="products">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div></>}