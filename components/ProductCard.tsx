"use client";import Link from "next/link";import {useCart} from "./CartProvider";import type {Product} from "../lib/products";
export function ProductCard({p}:{p:Product}){const{add}=useCart();return <article className="product"><Link href={`/shop/${p.id}/`}><div className={`pimage ${p.type}`}>{p.badge&&<span>{p.badge}</span>}</div></Link><div className="pinfo"><small>{p.category}</small><Link href={`/shop/${p.id}/`}><h3>{p.name}</h3></Link><strong>₹{p.price.toLocaleString("en-IN")}</strong> <del>₹{p.mrp.toLocaleString("en-IN")}</del><em>{p.stock<5?"Only "+p.stock+" left":"In stock"}</em><button disabled={!p.stock} onClick={()=>add(p)}>{p.stock?"Add to Cart":"Out of Stock"}</button></div></article>}